# check_ascii:
#   #!/usr/bin/env bash
#   for file in $(find . -type f -name "*.md" )
#   do
#     if diff -q --suppress-common-lines <(strings -w $file) $file; then
#         echo $file
#     fi
#   done
