[package]
name = "science"
version.workspace = true
authors.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Book code examples and their crate dependencies - science"
documentation.workspace = true
homepage.workspace = true
repository.workspace = true
license = "CC0-1.0"
keywords.workspace = true
categories = ["science"]
publish.workspace = true
autolib = false

[dependencies]
candle-core = "0.9.1"
candle-nn = "0.9.1"
linfa = "0.7.0"
linfa-linear = "0.7.0"
ndarray = "0.15.6" # pinned to 0.15.6 for linfa
smartcore = "0.4.0"
rand = { version = "0.10.1", features = ["std"] }

[features]
#default = []
# FIXME do we need a feature? candle = ["dep:candle-core", "dep:candle-nn"]
